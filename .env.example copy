# ─────────────────────────────────────────────────────────────────────────────
#  CoMS Server — .env.example
#  Safe to commit. Contains placeholder values only — never real credentials.
# ─────────────────────────────────────────────────────────────────────────────

PORT=5004
NODE_ENV=production

MONGO_URI=mongodb+srv://<user>:<password>@cluster0.xxxxx.mongodb.net/construction?appName=Cluster0

# node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=REPLACE_WITH_64_CHAR_RANDOM_STRING

CLIENT_ORIGIN=https://your-frontend.onrender.com
